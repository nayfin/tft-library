import { IsFormGroupConfigPipe } from './is-form-group-config.pipe';

describe('IsFormGroupConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormGroupConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
